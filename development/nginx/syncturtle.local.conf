map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

map $request_method $syncturtle_post_method_allowed {
    default 0;
    POST    1;
    OPTIONS 1;
}

map $request_method $syncturtle_get_method_allowed {
    default 0;
    GET     1;
    HEAD    1;
    OPTIONS 1;
}

server {
    listen 127.0.0.1:80;
    listen [::1]:80;
    server_name localhost;

    proxy_intercept_errors on;
    error_page 502 503 504 = @upstream_unavailable;

    location = /nginx-health {
        access_log off;
        default_type application/json;
        return 200 '{"status":"UP","component":"native-nginx"}\n';
    }

    # Browser action: http://localhost/syncturtle
    # Native Nginx preserves that exact Host/path/query for MinIO SigV4.
    location = /syncturtle {
        if ($syncturtle_post_method_allowed = 0) {
            return 405;
        }

        client_max_body_size 6m;
        client_body_timeout 5m;
        proxy_connect_timeout 5s;
        proxy_send_timeout 5m;
        proxy_read_timeout 60s;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Cookie "";
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:9000;
    }

    location ^~ /syncturtle/ {
        if ($syncturtle_get_method_allowed = 0) {
            return 405;
        }

        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 5m;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Cookie "";
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:9000;
    }

    location = /god-mode {
        return 308 /god-mode/$is_args$args;
    }

    location ^~ /god-mode/ {
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 1h;
        proxy_set_header Cookie "";
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:3001;
    }

    location /api/ {
        proxy_http_version 1.1;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:8000;
    }

    location /auth/ {
        proxy_http_version 1.1;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:8000;
    }

    location / {
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 1h;
        proxy_set_header Cookie "";
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_pass http://127.0.0.1:3000;
    }

    location @upstream_unavailable {
        default_type application/json;
        return 503 '{"error":"upstream_unavailable","message":"SyncTurtle is still starting or an upstream is unavailable"}\n';
    }
}
