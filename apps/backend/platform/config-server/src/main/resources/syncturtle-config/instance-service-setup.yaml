spring:
  main:
    web-application-type: none

  kafka:
    listener:
      auto-startup: false
    admin:
      fail-fast: true
      auto-create: true

app:
  outbox:
    relay:
      enabled: false
  product-telemetry:
    enabled: false
