spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/syncturtle_email
    username: email_app
    password: email_app_dev

  liquibase:
    user: email_migrator
    password: email_migrator_dev

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:19092}

app:
  services:
    instance-service:
      base-url: http://localhost:8001
