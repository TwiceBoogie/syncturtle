eureka:
  server:
    # dev/demo friendly
    enable-self-preservation: false

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
