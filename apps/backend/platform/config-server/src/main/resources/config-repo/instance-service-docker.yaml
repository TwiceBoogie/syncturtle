eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/syncturtle_instance
    username: instance_svc
    password: ${INSTANCE_DB_PASSWORD}
