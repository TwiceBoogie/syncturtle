server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    group:
      local:
        - native

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
